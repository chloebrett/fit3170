{
  "name": "backend-lambda",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "bookshelf": "^1.2.0",
    "knex": "^2.2.0",
    "pg": "^8.7.3"
  },
  "scripts": {
    "deploy": "yarn && cp ../backend-database/dbconfig.json dbconfig.json && serverless deploy"
  }
}
